const std = @import("std");
const show = std.debug.print;
const memory = std.mem;
const heap = std.heap;

pub fn main() !void {
    // idk zig man so this is one of my first commit so yeahh heppy hepyyyy
    show("this is a test", .{});
}
